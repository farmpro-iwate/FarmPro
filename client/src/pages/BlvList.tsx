import { useEffect, useState } from 'react';
import { Link as RouterLink } from 'react-router-dom';
import { Button, Card, CardContent, Chip, IconButton, Stack, Table, TableBody, TableCell, TableHead, TableRow, Typography } from '@mui/material';
import DeleteIcon from '@mui/icons-material/Delete'; import EditIcon from '@mui/icons-material/Edit'; import AddIcon from '@mui/icons-material/Add';
import { BlvTest } from '../types/blv'; import { deleteBlvTest, getBlvTestList } from '../services/blvApi'; import { daysUntil, judgeBlvNextTest } from '../utils/blv';
export function BlvList() { const [items, setItems] = useState<BlvTest[]>([]); const [loading, setLoading] = useState(true); const load = async () => { setLoading(true); setItems(await getBlvTestList()); setLoading(false); }; useEffect(() => { load(); }, []); const handleDelete = async (item: BlvTest) => { if (!window.confirm(`${item.cowName}のBLV検査記録を削除しますか？`)) return; await deleteBlvTest(item.id); await load(); }; const resultColor = (result: string) => result === '陽性' ? 'error' : result === '陰性' ? 'success' : 'warning'; const statusColor = (status: string) => status === '陽性管理' || status === '期限超過' ? 'error' : status === 'まもなく' ? 'warning' : 'default'; return <Stack spacing={2}><Stack direction="row" justifyContent="space-between" alignItems="center"><Typography variant="h5" fontWeight={800}>BLV管理</Typography><Button component={RouterLink} to="/blv/new" variant="contained" startIcon={<AddIcon />}>新規登録</Button></Stack><Card><CardContent>{loading ? <Typography>読み込み中...</Typography> : <Table size="small"><TableHead><TableRow><TableCell>対象牛</TableCell><TableCell>検査日</TableCell><TableCell>結果</TableCell><TableCell>次回検査</TableCell><TableCell>判定</TableCell><TableCell align="right">操作</TableCell></TableRow></TableHead><TableBody>{items.map((item) => { const status = judgeBlvNextTest(item.result, item.nextTestDate); return <TableRow key={item.id}><TableCell>{item.cowName}<br /><Typography variant="caption">{item.cowEarTag}</Typography></TableCell><TableCell>{item.testDate || '未検査'}</TableCell><TableCell><Chip size="small" label={item.result} color={resultColor(item.result) as any} /></TableCell><TableCell>{item.nextTestDate || '未定'}{item.nextTestDate && <><br /><Typography variant="caption">あと{daysUntil(item.nextTestDate)}日</Typography></>}</TableCell><TableCell><Chip size="small" label={status} color={statusColor(status) as any} /></TableCell><TableCell align="right"><IconButton component={RouterLink} to={`/blv/${item.id}/edit`}><EditIcon /></IconButton><IconButton color="error" onClick={() => handleDelete(item)}><DeleteIcon /></IconButton></TableCell></TableRow>; })}</TableBody></Table>}</CardContent></Card></Stack>; }
