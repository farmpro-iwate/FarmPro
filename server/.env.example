# Optional: absolute or relative directory for FarmPro runtime data.
# Keep real farm data outside source-controlled folders in production.
FARMPRO_DATA_DIR=./data

# Required before external publication. Use a long random value and never commit the real secret.
FARMPRO_AUTH_SECRET=replace-with-a-long-random-secret
